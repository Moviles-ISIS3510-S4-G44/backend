info:
  name: me
  type: http
  seq: 3

http:
  method: GET
  url: "{{server}}/auth/me"
  auth:
    type: bearer
    token: "{{mock_jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
