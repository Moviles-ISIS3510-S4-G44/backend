info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{server}}/auth/login"
  body:
    type: json
    data: |-
      {
        "identifier": "testuser",
        "password": "securepassword123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
